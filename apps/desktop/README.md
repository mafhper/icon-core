# IconCore Desktop (Tauri)

This app wraps `@iconcore/web` and exposes a native command `save_generated_files` for local folder export.

## Commands

- `npm run tauri:dev --workspace=@iconcore/desktop`
- `npm run tauri:build --workspace=@iconcore/desktop`

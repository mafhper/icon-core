# npm's resolver rejects some peer ranges that Bun tolerated (e.g.
# eslint-plugin-jsx-a11y declaring eslint ^3..^9 while the workspace uses
# eslint ^10). Keep the pre-migration dependency graph resolvable and
# reproducible in CI/release by opting out of strict peer resolution.
legacy-peer-deps=true
